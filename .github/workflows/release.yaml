on:
 push:
  tags:
    - 'v*.*.*'

name: Release

jobs:
  build:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Package
        run: ./release.sh
      - name: name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            release/*
